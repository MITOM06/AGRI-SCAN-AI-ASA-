{
  "name": "agri-scan-ai",
  "version": "1.0.0",
  "description": "AGRI-SCAN-AI — nền tảng quét ảnh cây trồng bằng AI: nhận diện bệnh/loài, wiki, chatbot RAG, thời tiết, my-garden, shop. Monorepo pnpm workspace.",
  "private": true,
  "license": "MIT",
  "author": "AGRI-SCAN-AI Team",
  "repository": {
    "type": "git",
    "url": "https://github.com/MITOM06/AGRI-SCAN-AI-ASA-.git"
  },
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "ai",
    "agriculture",
    "plant-disease-detection",
    "nestjs",
    "nextjs",
    "expo",
    "monorepo",
    "pnpm-workspace"
  ],
  "scripts": {
    "test": "pnpm -r run test",
    "dev:web": "pnpm --filter web dev",
    "dev:mobile": "pnpm --filter mobile start",
    "dev:all": "pnpm -r --parallel run dev",
    "build:packages": "pnpm --filter \"@agri-scan/database\" --filter \"@agri-scan/shared\" run build",
    "build:backend": "pnpm run build:packages && pnpm --filter backend run build",
    "build:web": "pnpm run build:packages && pnpm --filter web run build",
    "build": "pnpm run build:packages && pnpm --filter backend run build && pnpm --filter web run build"
  }
}
